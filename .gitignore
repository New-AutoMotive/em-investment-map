# JS/Node
node_modules/
npm-debug.log*
vite-debug.log*

# Build output
dist/

# Python virtual environment & compiled bytecode
database/venv/
__pycache__/
*.pyc
*.pyo

# Firebase service account credentials — NEVER commit
database/config/firebase-credentials.json

# Local backup & export directories (contents only — keep .gitkeep)
database/backups/*
!database/backups/.gitkeep
database/exports/*
!database/exports/.gitkeep

# OS / editor
.DS_Store
Thumbs.db
.env
.env.local
.env.*.local

# Jupyter / scratch notebooks
*.ipynb

# Working documents (large binaries, raw input files — not app source)
*.pdf
Charging Sector Data - Manufacturing sites.csv

# Logs
*.log
